
  # URide MVP Product Design

  This is a code bundle for URide MVP Product Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  